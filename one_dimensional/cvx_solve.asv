function [u_star, v_star] = cvx_solve(X, y)
    S = y > 0;
    Sc = y <= 0;
    keyboard;
    cvx_begin
        variable v
        varaible u(d)
         maximize(-0.5*sum_square(v*y(S) - u'*X(:,S)) + ...
             log(v) + sum(log_normcdf(v*y(S)' - u'*X(:,S))));
    cvx_end
end